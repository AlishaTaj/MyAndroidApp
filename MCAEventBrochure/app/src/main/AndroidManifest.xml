<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mcaeventbrochure">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MCAEventBrochure">

        <!-- Declare MainActivity with exported set to true for the launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- Required for Android 12 and above -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare BrochureActivity, not exported -->
        <activity
            android:name=".BrochureActivity"
            android:exported="false"></activity>
    </application>

</manifest>
